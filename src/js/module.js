export const wordField = [
    `const colors = ['#FF5733', '#33FF57', '#5733FF', '#FF33F9'];`,

    `const sum = (a, b) => a + b;`,

    `const greeting = 'Hello, World!';`,

    `document.querySelector('.my-element').innerHTML = 'New content';`,
    `const multiply = (a, b) => a * b;`,

    `document.getElementById('myButton').addEventListener('click', () => console.log('Button clicked!'));`,
    `fetch('https://api.example.com/data')
      .then(response => response.json())
      .then(data => console.log(data))
      .catch(error => console.error('Error fetching data:', error));`,

    `const car = { brand: 'Toyota', model: 'Camry', year: 2022 };`,

    `const temperature = 25;
    if (temperature > 30) {
      console.log('It\'s hot outside!');
    } else {
      console.log('It\'s a moderate temperature.');`,

    `const randomNumber = Math.floor(Math.random() * 100) + 1;`,

    `const firstName = 'Alice';`,

    `let age = 28;`,

    `const isWorking = true;`,

    `const hobbies = ['reading', 'coding', 'drawing'];`,

    `const favoriteNumbers = [7, 14, 21];`,

    `function checkTemperature(temp) {
        if (temp > 30) {
          return 'It\'s hot!';
        } else {
          return 'It\'s cool.';
        }
      }`,

      `const person = {
        name: 'Bob',
        greet: function() {
          console.log('Hello!');
        }
      };`,

      `const numbers = [1, 2, 3, 4, 5];
      for (const number of numbers) {
        console.log(number);
      }`,

      `button.addEventListener('click', () => alert('Button clicked!'));`,

      `const isAdult = age >= 18 ? true : false;`,
      
      `const randomNum = Math.floor(Math.random() * 100);`,
  ];